<SEMANTIC TAG_시맨틱 태그>

<header> 태그
- 헤더 영역
- 주로 사이트의 맨 위쪽이나 왼쪽에 위치
- 검색 창이나 사이트 메뉴 삽입

<nav> 태그
- 네비게이션 영역
- 같은 웹 문서 안에서 다른 위치로 연결하거나 다른 웹 문서로 연결하는 링크를 만든다

<main> 태그
- 웹 문서에서 핵심이 되는 내용
- 웹 문서에서 한번만 사용가능

<article> 태그
- 
